import { Button } from "react-native";

export const styles = {
    container : {
        flex: 1,
    },
    input: {
        height: 50,
        margin: 12,
        borderWidth: 1,
        borderRadius: 7,
        borderColor: '#999',
        paddingHorizontal: 16,
    },
    button: {
        margin: 12,
    }
}